# TODO: Устранение зависания GUI и ускорение импорта

## Контекст
Импорт большого НПА (npa_id=16012) «подвис» (перестал отвечать GUI). Причины:
1. `_log` вызывается из фонового потока и напрямую дёргает виджеты Tkinter (`insert`, `see`, `update_idletasks`) — Tkinter не потокобезопасен → deadlock/зависание mainloop.
2. Фаза 2 вставляет ревизии по одной с `ping()` на каждый вызов — медленно.
3. Нет обработки таймаутов/локов БД.

## Шаги
- [x] 1. Изучить код (`src/main.py`), JSON-файл и документацию.
- [x] 2. Согласовать план с пользователем.
- [x] 3. Сделать `_log` потокобезопасным (очередь + периодический `after`).
- [x] 4. Ускорить Фазу 2: пакетная вставка `npa_item_revision` + параграфов.
- [x] 5. Добавить retry при локах/таймаутах БД (коды 1205/1213).
- [x] 6. Проверить итоговый код (синтаксис/целостность).
